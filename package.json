{
  "name": "@anthropic/claude-agent-lifecycle",
  "version": "1.0.0",
  "description": "Persistent agent lifecycle management for Claude Code - ephemeral, turn, context, session, workflow, and project-scoped agents",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "test:unit": "bun test tests/unit",
    "test:integration": "bun test tests/integration",
    "build": "bun build src/index.ts --outdir=dist --target=node",
    "install-to": "bun install.ts"
  },
  "dependencies": {
    "claude-hooks-sdk": "^0.8.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "claude",
    "claude-code",
    "agent",
    "lifecycle",
    "persistent-agents",
    "subagents"
  ],
  "author": "Anthropic",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hgeldenhuys/claude-agent-lifecycle"
  }
}
